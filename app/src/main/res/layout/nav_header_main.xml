<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="@dimen/custom_height256dp"
    android:background="@drawable/nav_header_main_background"
    android:gravity="bottom"
    android:orientation="vertical"
    android:theme="@style/ThemeOverlay.MaterialComponents.Dark">


    <!--
    <ImageView
        android:id="@+id/image_tech"
        android:layout_width="@dimen/nav_size_image"
        android:layout_height="@dimen/nav_size_image"
        android:layout_gravity="center_horizontal"
        android:src="@drawable/ic_tech" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/nv_spacing_text"
        android:layout_marginBottom="@dimen/nv_spacing_text"
        android:orientation="vertical">

        <TextView
            android:id="@+id/nv_name"
            style="@style/TextAppearance.AppCompat.Widget.ActionBar.Title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:textColor="#FFFFFF"
            android:textSize="@dimen/primary_text_size" />

        <TextView
            android:id="@+id/nv_code"
            style="@style/TextAppearance.AppCompat.Widget.ActionBar.Subtitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:textColor="@color/colorWhite"
            android:textSize="@dimen/secondary_text_size" />



    </LinearLayout>
    -->
</LinearLayout>